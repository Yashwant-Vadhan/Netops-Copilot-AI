# Database URL - default uses SQLite locally, or Neon Postgres URL in production
DATABASE_URL=sqlite:///./netops.db
# Shared secret for telemetry ingestion
COLLECTOR_SECRET=netops-collector-secret-key-2026
# Environment name
ENVIRONMENT=development
